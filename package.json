{
  "name": "car-rental-platform",
  "version": "1.0.0",
  "description": "A modern car rental platform with booking and driver services",
  "scripts": {
    "setup": "cd backend && npm install && cd ../frontend && npm install",
    "seed": "cd backend && node seedData.js",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "start:backend": "cd backend && npm start",
    "build:frontend": "cd frontend && npm run build"
  },
  "keywords": ["car-rental", "booking", "react", "nodejs", "mongodb"],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  }
}